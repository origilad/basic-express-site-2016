extend layout

block prepend links
      - var homeURL = '/Home/' + user.id;
      - var adURL = '/AdventureAnalytics/' + user.id;
      - var travelURL = '/TravelLog/' + user.id;

block content
  p Random adventure page

  div(class="container")
    div(class="jumbotron")
      h1 #{adventureName}
